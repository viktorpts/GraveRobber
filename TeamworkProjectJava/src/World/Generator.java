package World;

/**
 * Place map-making code here. Class is static cause we only need it to make the geometry that is then contained in the Level
 */
public class Generator {
    // Implement this method, make it return whatever is necessary for initial display. Change the return type as needed
    public static boolean[] Generate() {
        boolean[] result = {false, false, false, false};
        return result;
    }
}
