package Models;

public class LootTable {
    // TODO: The table is basically a map of Loot linked to weight values, representing likelihood of dropping
    // This will suck with dynamically generated items, so we'll leave item drops and inventory for the end
}
